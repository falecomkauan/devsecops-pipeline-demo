# Dependencies pinned to OLD versions on purpose, so the SCA scanners
# (Grype / Trivy) have known CVEs to report. In a real project these
# would be kept up to date.
#
# Examples of intentionally outdated packages with publicly known CVEs:

requests==2.19.1
pyyaml==5.1
jinja2==2.10
urllib3==1.24.1
flask==0.12.2
