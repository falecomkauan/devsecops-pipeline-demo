# Intentionally Vulnerable Sample App

The code in this folder contains **deliberate** security flaws so that the
pipeline's scanners have something to detect. It is a teaching fixture, not an
example of how to write secure code.

See `vulnerable_example.py` - each flaw is annotated with the weakness it
demonstrates and how to fix it properly.
